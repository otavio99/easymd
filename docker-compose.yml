version: '3.7'

volumes:
    logs:
        driver: local

services:
    slim:
        build: .
        command: php -S 0.0.0.0:8080 -t public
        ports:
            - 8080:8080
        volumes:
            - .:/var/www
            - logs:/var/www/logs
